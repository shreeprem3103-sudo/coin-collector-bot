name: Mobile App Automation Bot

on:
  workflow_dispatch: 
  schedule:
    - cron: '0 * * * *' 

jobs:
  run-bot:
    runs-on: macos-14
    
    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Automation Tools
      run: |
        pip install Appium-Python-Client selenium
        npm install -g appium
        appium driver install uiautomator2

    - name: Launch Android Cloud Emulator & Run Bot
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 30 # Android 11.0 (Highly optimized for ARM64 runners)
        target: google_apis
        arch: arm64-v8a # Matches the M1/M2 hardware architecture
        script: |
          appium --log appium.log &
          sleep 5 
          python bot31.py
